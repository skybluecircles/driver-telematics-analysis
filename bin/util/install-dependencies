#!/bin/bash

if hash cpanm >/dev/null 2>&1
then
    cat cpan.dependencies | cpanm
else
    cat >&2 <<message
All of the dependencies are in (cpan.dependencies)

You can install them however you'd like, but I'd highly
recommend installing 'cpanm' first and rerunning this script.

https://github.com/miyagawa/cpanminus
message

    exit 1
fi
