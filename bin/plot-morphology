#!/bin/bash

set -e
set -o pipefail

driver=${1-1}

commands=(
    orig-rotated-coordinates
    rotated-coordinates
    rotated-coordinates-with-calipers
    rotated-coordinates-just-calipers
)

for command in ${commands[@]}
do
    echo "$command"
    bin/plot/$command "$driver"
done
